// All type definitions are now centralized in @web-agent/shared-types.
// Re-export them from the shared package for local use if needed by other frontend files,
// or update direct imports in other files to point to @web-agent/shared-types.

export * from '@web-agent/shared-types';
